import React, {useState} from 'react';

function Hook(props)  {
    const compteurState = useState(0);
    return ( 
        <React.Fragment>

        </React.Fragment>
     );
}
 
export default Hook;